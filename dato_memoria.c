#include <stdio.h>

int main() {
	//declarar varaiable
	int miNumero = 10;
	printf("imprimir el calor de la variable: %d", miNumero);
	printf("\nimprimir la direccion de memoria: %p", miNumero);
	return 0;
}

